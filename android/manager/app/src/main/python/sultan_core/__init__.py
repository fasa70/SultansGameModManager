"""Host-test fallback for the Android adapter.

The APK supplies the compiled Chaquopy sultan_core extension. This fallback is
only for exercising conflict collection and allocation on a development host;
it deliberately does not implement JsonDoc or json_ops.
"""
